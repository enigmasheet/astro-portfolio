---
import { HERO, SITE } from '../../data/site';
import SocialLinks from '../ui/SocialLinks.astro';
const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase : rawBase + '/';
const phrases =
  'Backend Software Developer|Product-Oriented Technologist|Full-Stack Web Developer|Problem Solver';
const firstPhrase = 'Backend Software Developer';
---

<section class="relative min-h-[calc(100vh-64px)] flex items-center overflow-hidden">
  <div class="absolute inset-0 pointer-events-none overflow-hidden" aria-hidden="true">
    <div
      class="absolute -top-40 -right-40 w-96 h-96 bg-primary/10 rounded-full blur-[100px]"
      style="animation: heroOrbA 20s ease-in-out infinite"
    >
    </div>
    <div
      class="absolute -bottom-40 -left-40 w-96 h-96 bg-secondary/10 rounded-full blur-[100px]"
      style="animation: heroOrbB 25s ease-in-out infinite"
    >
    </div>
    <div
      class="absolute inset-0 opacity-[0.03]"
      style="background-image: radial-gradient(circle, currentColor 1px, transparent 1px); background-size: 40px 40px;"
    >
    </div>
  </div>
  <div class="max-w-6xl mx-auto px-4 py-24 relative z-10 w-full">
    <div class="max-w-3xl">
      <p
        class="text-sm font-semibold tracking-widest uppercase text-primary mb-4"
        style="animation: heroFadeIn 0.6s ease 0.1s forwards; opacity:0;"
      >
        {HERO.greeting}
      </p>
      <h1
        class="text-4xl md:text-6xl lg:text-7xl font-bold text-(--color-text) mb-4 leading-tight"
        style="animation: heroFadeIn 0.6s ease 0.2s forwards; opacity:0;"
      >
        {SITE.fullName}
      </h1>
      <div
        class="text-xl md:text-2xl text-text-secondary mb-6 min-h-8"
        style="animation: heroFadeIn 0.6s ease 0.3s forwards; opacity:0;"
      >
        <span
          data-typing
          data-phrases={phrases}
          data-speed="80"
          data-delete-speed="40"
          data-pause="2500"
          data-delay="1200">{firstPhrase}</span
        >
      </div>
      <p
        class="text-lg text-text-secondary max-w-xl leading-relaxed mb-8"
        style="animation: heroFadeIn 0.6s ease 0.4s forwards; opacity:0;"
      >
        {HERO.description}
      </p>
      <div
        class="flex flex-wrap gap-4"
        style="animation: heroFadeIn 0.6s ease 0.5s forwards; opacity:0;"
      >
        <a
          href={base + 'projects/'}
          class="inline-flex items-center px-6 py-3 rounded-lg bg-primary text-white font-medium hover:bg-primary-dark transition-colors no-underline"
        >
          {HERO.primaryCta}
        </a>
        <a
          href={base + 'contact/'}
          class="inline-flex items-center px-6 py-3 rounded-lg border-2 border-primary text-primary font-medium hover:bg-primary/5 transition-colors no-underline"
        >
          {HERO.secondaryCta}
        </a>
      </div>
      <div class="mt-12" style="animation: heroFadeIn 0.6s ease 0.6s forwards; opacity:0;">
        <SocialLinks />
      </div>
    </div>
  </div>
</section>
